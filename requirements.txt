# ─────────────────────────────────────────────────────────────────────────────
# ClimaCity Paris -- dépendances Python
#
# Versions épinglées pour la reproductibilité.
# Si vous mettez à jour PySpark, vérifiez la compatibilité de delta-spark :
#   Spark 3.5.x  →  delta-spark 3.2.x
#   Spark 3.4.x  →  delta-spark 2.4.x
# ─────────────────────────────────────────────────────────────────────────────

# ── Delta Lake (Python bindings -- les JARs sont dans le Dockerfile) ─────────
delta-spark==3.2.0

# ── Suivi d'expériences ML ───────────────────────────────────────────────────
mlflow==2.17.2

# ── Visualisation ────────────────────────────────────────────────────────────
folium==0.17.0
plotly==5.24.1
ipywidgets>=8.0.0          # widgets interactifs dans JupyterLab

# ── Données météo ─────────────────────────────────────────────────────────────
openmeteo-requests==1.3.0
requests-cache==1.2.1      # cache HTTP pour les appels répétés à Open-Meteo
retry-requests==2.0.0

# ── Utilitaires ──────────────────────────────────────────────────────────────
tqdm==4.66.5               # barres de progression (téléchargements)
requests==2.32.3
pyarrow==17.0.0            # lecture/écriture Parquet depuis Pandas
fastparquet==2024.5.0      # alternative légère à pyarrow pour Pandas

# ── Déjà présents dans l'image de base (listés pour documenter les versions) ─
# pyspark==3.5.3
# pandas>=2.0.0
# numpy>=1.26.0
# scikit-learn>=1.3.0
# matplotlib>=3.8.0
# jupyterlab>=4.0.0
